mixin preview(img, name, className, classGrid)
	a(href="img/" + img class=`preview ${className} ${classGrid}` data-fancybox="gallery")
		figure.preview__img
			img(src="img/" + img, alt= name)
		.preview__name #{name}